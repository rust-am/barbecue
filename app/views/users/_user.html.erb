<span class="user">
  <span class="user-avatar">
    <%= image_tag(user_avatar(user), class: 'img-icon') %>
  </span>
  <span class="user-name">
    <% if request.path == user_url(user) %>
      <%= user.name %>
    <% else %>
      <%= link_to user.name, user_url(user) %>
    <% end %>
  </span>
</span>
